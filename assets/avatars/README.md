# Avatares LAqP

Estos SVG son placeholders predeterminados para perfiles de usuario. Podés reemplazarlos por PNG manteniendo los mismos IDs en `js/auth.js` o agregar más opciones a `DEFAULT_AVATARS`.
